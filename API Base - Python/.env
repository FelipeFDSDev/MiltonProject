# Email account used as sender for SMTP
EMAIL_USER=felipefrs2007@gmail.com
# EMAIL_PASS deve ser sua App Password (Google App Password ou senha do provedor SMTP)
EMAIL_PASS=zyao lynl qvdk vycj

# SMTP configuration (personalize se usar Mailtrap ou outro servidor SMTP)
# Exemplo para Gmail (padrão): smtp.gmail.com:587
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
# Use TLS (True/False)
SMTP_USE_TLS=True

# Example: Mailtrap (para testes)
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=2525
# EMAIL_USER=<mailtrap_user>
# EMAIL_PASS=<mailtrap_pass>
